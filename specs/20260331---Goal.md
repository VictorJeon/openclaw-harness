# Goal
Phase 1 surface refactor for /Users/nova/.openclaw/extensions/openclaw-harness.

Goal: Make harness_execute the unmistakable primary path for coding work, and mark the legacy direct-Claude session surface as legacy/deprecated without deleting it.

Context: This plugin contains two coexisting layers:
1) Active: harness_execute (tier 0/1/2, claude-realtime.sh for tier 2)
2) Legacy: harness_launch, harness_sessions, harness_kill, harness_output, harness_fg, harness_bg, harness_respond, harness_stats, /claude* commands, claude-launch.ts guards

This is NOT a full rewrite. Do NOT remove SessionManager/NotificationRouter. Do NOT change harness_execute architecture. Do NOT remove legacy tools.

Required changes:
1. Update tool descriptions for the legacy tool family: add [LEGACY] prefix, say to prefer harness_execute.
2. Update /claude* command descriptions with same positioning.
3. Update README.md, docs/ARCHITECTURE.md to clearly distinguish primary vs legacy path.
4. Reposition docs/safety.md as legacy-launch safety guidance.
5. Add enableLegacyTools config flag in openclaw.plugin.json schema (default true, for future gating).

Verification: npm run build succeeds, legacy descriptions contain LEGACY/DEPRECATED, docs show harness_execute as primary.

# Scope
Active: harness_execute (tier 0/1/2, claude-realtime.sh for tier 2)

# Acceptance Criteria
- Active: harness_execute (tier 0/1/2, claude-realtime.sh for tier 2)

# Context
Working directory: /Users/nova/.openclaw/extensions/openclaw-harness
Task ID: task-1
Harness plan: plan-20260331-vonfxq

---
## 작업 완료 후 필수
- CLAUDE.md가 있으면 이번 작업에서 배운 패턴/규칙/주의사항을 추가할 것
- CLAUDE.md가 없으면 새로 생성할 것
